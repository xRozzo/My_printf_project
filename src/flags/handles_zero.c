/*
** EPITECH PROJECT, 2022
** handle_hash.c
** File description:
** handle the 0 flag
*/
/*
#include <stdio.h>
#include <stdarg.h>
#include <stddef.h>
#include <stdlib.h>
#include <stdbool.h>
//#include "my.h"
//#include "fnc_ptr.h"

//get the result of with --> so array is the result of with the array padded.
char *handle_0(char *arr) {
    int i = 0;
    while (arr[i] != '\0') {
        if (arr[i] == ' '){
            arr[i] == '0';
        }
        i++;
    }
}

int main() {
printf("%010.5d", 5);
}

// so only if argument == arr is only num.

*/